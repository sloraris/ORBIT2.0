---
- name: Variable test
  gather_facts: false
  hosts: server_dns

  tasks:

    - name: Debug variables
      ansible.builtin.debug:
        msg:
          - "adguard_config_url: {{ adguard_config_url }}"
          - "unbound_config_url: {{ unbound_config_url }}"
          - "nm_template_url: {{ nm_template_url }}"
          - "adguard_pswd: {{ adguard_pswd }}"
